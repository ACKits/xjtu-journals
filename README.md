# Excel 数据检索工具

基于 Vue 3 + Vite 构建，支持上传多个 Excel 文件，并提供模糊搜索、文件筛选功能。

## 功能特性
- 上传 `.xlsx` / `.xls` 文件（支持多选）
- 自动解析并展示所有数据
- 模糊搜索（在所有列中匹配关键词）
- 按文件筛选（单选/多选）
- 表格表头固定，支持横向滚动

## 安装与运行
```bash
npm install
npm run dev